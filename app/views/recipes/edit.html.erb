<% provide(:title, 'Nova receita') %>
<% provide(:button_text, 'Salvar') %>
<% provide(:fa_icon, 'save lg') %>

<section id="edit-recipe">
  <div class="row">
    <div class="col-md-8 col-md-offset-2">
      <h2>Editar Receita</h2>
      <hr>
      <%= render 'form' %>
    </div>
  </div>
</section>
